% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cases.R
\name{validate_cases}
\alias{validate_cases}
\title{Check the invariants every case must satisfy}
\usage{
validate_cases(ids = list_cases()$id)
}
\arguments{
\item{ids}{Cases to check (default: all).}
}
\value{
A \code{data.frame} with one row per case and a logical \code{ok} column.
}
\description{
Structural gate (dataset is public; every claim is GROUNDED in the case's
\code{verification_report.json}; \code{n_grounded == n_claims}; a byte-identical replay is
recorded; a \code{verified}/\code{escalated} case has all claims grounded). The
pre-registration hash and per-case number tracing are checked by
\code{tools/validate_case.py} in CI.
}
