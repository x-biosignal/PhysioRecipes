# PhysioAgent PoC -- provenance bundle

Generated by the PhysioAgent PoC substrate. One real hypothesis run end-to-end through the constrained-agent substrate.

- **Dataset**: MIT-BIH Arrhythmia Database, record 100 (MLII/V5, 360 Hz) (offline; sha256 in `proposal.json`).
- **Hypothesis**: MIT-BIH record 100 exhibits a normal resting heart rate.
- **Result**: mean HR = 72.03 bpm (MLII); decision 60<=HR<=100 MET.
- **Prereg hash (SUBS-02)**: `e2bd62714b2acb6df8b7dd8fd3995719a33878647d70b5bff21241b2762bdf41` (algo sha256, registered_at 2026-08-10T00:00:00Z).
- **Terminal artifact hash (SUBS-03)**: `deb3c8f75d54db9c` (xxhash64); replay all_match = TRUE.
- **Claims (SUBS-01)**: 2/2 grounded, 0 artifact-mismatch.
- **Adherence**: TRUE (executed run vs frozen plan).

## Files
- `proposal.json` -- LLM operator's hypothesis + typed op-DAG (non-deterministic; frozen next).
- `prereg.json` -- SUBS-02 frozen confirmatory plan (+ sha256 freeze).
- `run_manifest.json` -- SUBS-03 op-DAG + output hashes + environment fingerprint.
- `terminal_table.csv` -- the terminal statistic (content-addressed).
- `report.md` -- human-readable report citing only artifact-backed numbers.
- `claims.json` / `verification_report.json` -- SUBS-01 claim registry + verdict.

HONESTY: the substrate certifies reproducibility and auditability, not scientific correctness (expert judgement); the agent proposal is non-deterministic, which is why it is frozen before results.
